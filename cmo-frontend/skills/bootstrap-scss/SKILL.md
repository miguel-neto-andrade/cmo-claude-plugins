---
name: bootstrap-scss
description: Use when working with Bootstrap 5 + custom SCSS in C-Mo frontend projects — utility class usage, theming via SCSS variables, custom overrides, and SCSS file structure.
---

# Bootstrap 5 + SCSS

TODO — fill in C-Mo Bootstrap 5 + SCSS rules. Topics to cover:

- SCSS entry point and file structure (`_variables.scss`, `_overrides.scss`, partials).
- How to extend / customise Bootstrap without forking it.
- Utility classes vs custom SCSS — when to reach for which.
- Theming: brand variables, colour palette, spacing scale.
- Anti-patterns (deep selector nesting, `!important`, inline styles).
